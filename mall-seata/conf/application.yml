server:
  port: 20300

spring:
  application:
    name: mall-seata

logging:
  config: classpath:logback-spring.xml
  file:
    path: ${user.home}/logs/seata

console:
  user:
    username: seata
    password: seata

seata:
  config:
    type: nacos
    nacos:
      server-addr: localhost:20200
      group: DEFAULT_GROUP
      namespace: 
      username: jymj-nacos
      password: jymj-nacos
      data-id: mall-seata-server.properties
  registry:
    type: nacos
    nacos:
      application: mall-seata
      server-addr: localhost:20200
      group: MALL-MANAGE
      username: jymj-nacos
      password: jymj-nacos
  security:
    secretKey: SeataSecretKey0c382ef121d778043159209298fd40bf3850a017
    tokenValidityInMilliseconds: 1800000
    ignore:
      urls: /,/**/*.css,/**/*.js,/**/*.html,/**/*.map,/**/*.svg,/**/*.png,/**/*.ico,/console-fe/public/**,/api/v1/auth/login